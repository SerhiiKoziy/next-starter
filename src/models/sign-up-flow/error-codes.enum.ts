export enum KboErrorCodes {
  COMPANY_EXISTS = 'Company already exists',
  BCE_WRONG_CODE = "Company code doesn't match",
  BCE_404 = "Company doesn't exist in BCE",
  BCE_DOWN = 'BCE not available',
}
